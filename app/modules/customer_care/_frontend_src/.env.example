# Frontend → Backend URL for the dev proxy. Default: http://127.0.0.1:8000
# In production the SPA is served from the same origin as the API, so this
# only matters for `npm run dev`.
VITE_BACKEND_URL=http://127.0.0.1:8000
